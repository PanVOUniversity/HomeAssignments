#include <iostream>
#include <fstream>
#include <string>
std::ifstream infile;

int main()
	{
		std::string a;
		std::cin >> a;
		auto b = sizeof(a);
		std::cout <<  b;
		std::ofstream fout(txt.bin, ios_base::binary); //create class ofstream object
		string h = infile.open(a ,std::ios::binary|std::ios::in);
		
	}
